请将提供的长文本拆分成多个较小的段落，以便更好地管理和处理，同时保持内容的连贯性和上下文完整性。
将拆分后的文本转化为多个"问答对"，并以指定的 JSON 格式输出。

## 严格要求

1. 仅输出 JSON 格式内容，不要附加任何解释、说明或多余文字。
2. 输出内容必须完整、符合 JSON 语法且可解析。
3. 拆分时应尽可能保留原文的语义与细节。
4. 每个"instruction"字段应是一个有意义的自然语言问题，不能只是词语或短语。
5. "output"字段应是对应问题的回答内容。
6. 对应问题的回答内容必须可以在原始文本找到。
7. "instruction"和"output"字段都是字符串，不要设置为其它类型。
8. 对应问题的回答内容必须可以在原始文本找到。

## 输出格式

```json
[
  {
    "instruction": "问题",
    "output": "答案"
  },
  {
    "instruction": "问题",
    "output": "答案"
  }
]
```

## 待处理内容

%s

